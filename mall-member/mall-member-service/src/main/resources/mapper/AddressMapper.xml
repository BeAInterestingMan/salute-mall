<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.salute.mall.member.service.mapper.AddressMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.salute.mall.member.service.pojo.entity.Address" id="baseMap">
        <result property="id"  column="id"/>
        <result property="addressCode"  column="address_code"/>
        <result property="userCode"  column="user_code"/>
        <result property="receiver"  column="receiver"/>
        <result property="provinceCode"  column="province_code"/>
        <result property="provinceName"  column="province_name"/>
        <result property="areaCode"  column="area_code"/>
        <result property="areaName"  column="area_name"/>
        <result property="cityCode"  column="city_code"/>
        <result property="cityName"  column="city_name"/>
        <result property="addressCodePath"  column="address_code_path"/>
        <result property="detailAddress"  column="detail_address"/>
        <result property="address"  column="address"/>
        <result property="postCode"  column="post_code"/>
        <result property="mobile"  column="mobile"/>
        <result property="sort"  column="sort"/>
        <result property="version"  column="version"/>
        <result property="creator"  column="creator"/>
        <result property="creatorCode"  column="creator_code"/>
        <result property="createdTime"  column="created_time"/>
        <result property="modifier"  column="modifier"/>
        <result property="modifierCode"  column="modifier_code"/>
        <result property="modifiedTime"  column="modified_time"/>
    </resultMap>

    <sql id="baseColumn">
        id,
        address_code,
        user_code,
        receiver,
        province_code,
        province_name,
        area_code,
        area_name,
        city_code,
        city_name,
        address_code_path,
        detail_address,
        address,
        post_code,
        mobile,
        sort,
        version,
        creator,
        creator_code,
        created_time,
        modifier,
        modifier_code,
        modified_time
    </sql>
</mapper>